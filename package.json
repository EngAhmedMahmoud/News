{
  "name": "server-send-event",
  "version": "1.0.0",
  "description": "Using server send event as one way to get server updates when these updates are available.It comes automatically from the server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "EngAhmedMahmoud",
  "license": "ISC"
}
